

CREATE TABLE item_909 (
    id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    name VARCHAR2(100),
    price NUMBER,
    total_number NUMBER,
    PRIMARY KEY (id)
);

SELECT * FROM ITEM_909 i ;

ALTER TABLE item_909 ADD ACTIVE NUMBER DEFAULT 1;



CREATE TABLE ITEM_DETAILS_909 (
    ID NUMBER PRIMARY KEY,
    description VARCHAR2(255),
    ISSUE_DATE DATE,
    EXPIRY_DATE DATE,
    CONSTRAINT FK_ITEM_DETAILS
        FOREIGN KEY (ID)
        REFERENCES ITEM_909(ID)
        ON DELETE CASCADE
);

INSERT INTO ITEM_DETAILS_909 (ID, description, ISSUE_DATE, EXPIRY_DATE)
SELECT ID, NULL, NULL, NULL
FROM ITEM_909
WHERE ID NOT IN (SELECT ID FROM ITEM_DETAILS_909);	

SELECT * FROM ITEM_DETAILS_909;

CREATE TABLE USERS_909 (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(100) NOT NULL,
    EMAIL VARCHAR2(100) NOT NULL UNIQUE,
    PASSWORD VARCHAR2(100) NOT NULL
);
SELECT * FROM USERS_909;

